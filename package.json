{
  "name": "mrank-api",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=10.16.0"
  },
  "engineStrict": true,
  "dependencies": {
    "json-server": "^0.15.0"
  },
  "scripts": {
    "start": "json-server --watch db.json"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
